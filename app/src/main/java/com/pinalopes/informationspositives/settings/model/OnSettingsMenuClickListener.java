package com.pinalopes.informationspositives.settings.model;

import androidx.appcompat.widget.SwitchCompat;

public interface OnSettingsMenuClickListener {
    void onMenuItemSelected(int position, SwitchCompat notificationSwitch);
}
